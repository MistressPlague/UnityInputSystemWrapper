# UnityInputSystemWrapper
A wrapper to make using the new unity input system package from C# just like the old. This will only work if the legacy input module isn't referenced, as it uses the namespace.
